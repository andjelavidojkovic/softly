import React from 'react';

import LoginProvider from './General/Login.provider';

const Wrapper = () => {
  return <LoginProvider>{/* <LoginWrapper /> */}</LoginProvider>;
};

export default Wrapper;
